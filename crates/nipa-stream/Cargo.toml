[package]
name = "nipa-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ffprobe/ffmpeg sidecar、播放判定、HLS session（stub）"

# 本 crate 不回流客户端（§3.1），但 M0 阶段同样保持零重依赖：
# ffmpeg/ffprobe 走 sidecar 子进程（§2.3），不做 FFI 绑定。

[dependencies]
nipa-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
